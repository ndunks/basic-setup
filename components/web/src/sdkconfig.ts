
var sdkconfig: Record<string, any> = globalThis.sdkconfig = {
    CONFIG_APP_WIFI_MAX_CLIENT: 5,
    CONFIG_APP_WIFI_MAX_RETRY: 5,
    CONFIG_APP_WIFI_ACTION_TIMEOUT: 5000,
    CONFIG_APP_WITH_SENSOR: true,
    CONFIG_APP_WITH_COMMANDS: true
}
